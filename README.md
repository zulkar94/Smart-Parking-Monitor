# Smart-Parking-Monitor
Python parking system. Detects Bangla + English plates via OpenCV + EasyOCR, normalizes both scripts to one key, assigns nearest free slot, gives turn-by-turn guidance. FastAPI + SQLite backend, live web dashboard. Swap detector for YOLOv8, hook real camera feed to extend.
